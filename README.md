# 🎥 YouTube Manager (SQLite3 Edition)

A robust Command Line Interface (CLI) application built with Python that uses a **SQLite3 database** to manage a collection of YouTube video metadata. 

## ✨ Features
- **Persistent Storage**: Uses `sqlite3` to save data in a local `.db` file.
- **CRUD Operations**: Complete Create, Read, Update, and Delete functionality.
- **Parameterized Queries**: Built with security in mind to prevent SQL injection.
- **Auto-ID Management**: Uses Primary Keys for accurate record tracking.

## 🚀 How to Use
1. **Clone the repository:**
   ```bash
   git clone [https://github.com/YOUR_USERNAME/youtube-manager-sqlite.git](https://github.com/YOUR_USERNAME/youtube-manager-sqlite.git)
